import { css } from '@macrostyles/core';
import { modularScale } from 'polished';

export const fontSize: string = modularScale(2);

export const divBackGround = css`
  background-color: aquamarine;
`;

export const composed = css`
  color: red;
`;
